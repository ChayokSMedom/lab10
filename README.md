Команда vagrant up не была выполнена из-за отсутствия поддержки вложенной виртуализации (VT-x недоступен в гостевой системе). Это подтверждается командой egrep -c '(vmx|svm)' /proc/cpuinfo, которая вернула 0.
